# dependencies
node_modules

# build output
dist

# logs
*.log
npm-debug.log*

# env files
.env
.env.local
.env.*.local

# runtime data (live counters / submissions) — keep seeds committed, not live state
data/receipt-counter.json
server/data/
